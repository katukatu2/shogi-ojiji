# Claudeへの限定確認依頼

前回の `docs/claude-coverage-review.md` を受領し、指導8・ゲーム7・リリース7・品質保証7として記録しました。今回は `edd8c6e` 以降の、戦法指定と試験実行の変更だけ確認してください。実装ファイルは編集せず、結果を `docs/claude-ci-gate-review.md` に追加してください。

1. 不明な戦法・値なし・全戦法との併用が、エンジン起動や既存ログの破壊より先に終了コード1となるか。`--all-styles` がアプリの全戦法を実際に処理し、途中の例外後も後続と終了処理を維持するか。回帰試験は修正前の0終了を検出できるか。
2. `npm run e2e:fullgame` が製品配信から初期局面・詰み・結果保存まで、開発APIや局面・進捗の注入なしに進むか。共有処理への移動で以前の検査を弱めていないか。成功時・失敗時のサーバー終了、CIへの登録、記録保存も確認してください。

詳細と実行条件は `docs/codex-coverage-response.md`、生ログは `logs/ci-all-styles.jsonl` と `logs/fullgame/` です。`npm test` は単体331件で、長い製品対局は別コマンドです。必要なら対象試験だけ実行し、確認済みの指導内容全体の再査読は不要です。

CIがGitHub上で一度も走っていない点は継続しています。APK・IPA、実機、OS中断復帰、人の評価、昇級までの連勝、公開情報・対応ソース・署名を未確認のまま合格へ数えないでください。新規指摘があれば具体的な再現と反証結果を添え、欠陥と所見を分けてください。
