# Claude 独立評価（現況）2026-09-14

査読者: Claude（実装ファイルは編集していない。追加したのはこのファイルだけ）
対象: `d74cf72`（`origin/main` と一致）

## 4 領域の評価

| 領域 | 評価 | 前回 | 8 までの残条件 |
|---|---|---|---|
| 指導の信頼性 | 8 | 8 | （到達済み。台詞の採取と目視を続ける運用が要る） |
| ゲーム完成度 | 7 | 7 | 実機、OS 中断復帰、人が遊んだ評価の反復、昇級までの連勝、「決め手」の実表示 |
| リリース準備 | 7 | 7 | 公開先 URL と連絡先、署名、実機、ストア提出物、対応ソースの提供方法 |
| 品質保証 | 8 | 8 | （到達済み。保守課題として main.ts 1,131 行） |

## 前回の記録からの変化

前回の査読（[claude-playtest-review.md](claude-playtest-review.md)）で保留していた 2 点は解消した。

1. **`d74cf72` は push 済みで、GitHub CI が緑になっている。** 前回「未 push のため CI 未検証」と書いた状態は解消した。
2. **Android と iOS のワークフローも走っており、成果物が出ている。** 前回の記録に「`android.yml` は未実行」と書いたのは誤りで、`0679c1f` の時点で既に実行され成功していた。訂正する。

## 確認した事実（`d74cf72`）

### CI（run 34760420212、7分51秒）

21 ステップすべて success。`skipped` も `continue-on-error` も無い。

| 検査 | 件数 |
|---|---|
| 単体 | 19 ファイル・**349 件成功** |
| 操作（e2e） | **56 件成功**（3.1 分） |
| PWA | **24 件成功**（1.2 分） |
| 製品通し | 1 件成功。`{"plies":59,"winner":"player","decisive":0,"endedByMate":true}` |
| 自動対局 | 5 戦法 × 2 局 |

成果物 5 件（playwright-report / pwa-report / product-fullgame / selfplay-logs / dist）が実在する。

### Android（run 34760420214、1分33秒）

15 ステップすべて success。`assembleDebug` が通り、**`app-debug-apk` 4,827,479 バイトが実在する**。`if-no-files-found: error` なので空ではない。ただし **debug・未署名**であり、リリース用 aab ではない。

### iOS（run 34760420205、42秒）

`xcodebuild -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO` が通り、**`ios-simulator-app` 2,482,318 バイトが実在する**。シミュレータ用であり、**IPA でも実機用でもない**。

### 公開情報

`npm run release:check` は依然として 4 か所で失敗する。

```
- src/main.ts: 公開情報が未確定
- public/privacy.html: 公開情報が未確定
- docs/store/listing.md: 公開情報が未確定
- README.md: 公開情報が未確定
```

**これは意図された失敗である。** 仮の URL で通してはいけない。

## 評価を据え置く理由

### ゲーム完成度 7

APK と iOS シミュレータ用のビルドが CI で作れるようになったのは前進で、**実機確認の道具が揃った**。しかし残条件は「実機で遊ぶこと」自体であり、**ビルドが作れることと遊んで確かめたことは別である**。OS 中断復帰、昇級までの連勝も未着手。

**「決め手」は今回も 0 件。** これまでの全実行で一度も表示されていない。指導の中心機能の一つが、実際に出るところを誰も見ていない。

### リリース準備 7

残条件 4 つのうち、片付いたのは「APK・IPA の生成」の半分だけである。

- 公開先 URL と連絡先: 未定（ユーザーの判断待ち）
- 署名: 未着手。debug APK は配布できない
- 実機: 未着手
- ストア提出物と対応ソースの提供方法: 未着手。GPLv3 の対応ソース提供義務はバイナリ配布の時点で生じる

### 品質保証 8

前回 8 に上げた根拠（CI がゲートとして機能している）が、**最新コードでも維持されていることを確認した**。上積みの材料は無い。`main.ts` は 1,068 → 1,131 行に増えており、保守課題は残る。

## 次の一手（優先順）

1. **`app-debug-apk` を Android 実機に入れて遊ぶ。** ゲーム完成度とリリース準備の両方に効く、いま最も安い前進。
2. **公開先を決める。** これが決まらないと `release:check` が通らず、リリース準備は 7 から動かない。
3. 「決め手」が実際に出る条件を調べる。出ないのが正しいのか、条件が厳しすぎるのかを切り分ける。
