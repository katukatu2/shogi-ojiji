# Claudeへの再査読依頼

以下をClaude Codeへ渡してください。

---

「将棋オジジの定石指南」の18指摘にCodexが対応しました。実装を変更せず、修正差分の再査読をお願いします。

保存先: `C:\Users\sonot\Documents\Codex\2026-09-04\shogi-ojiji`
比較起点: `e0b2b69`（前回の査読対象）。`git status --short` と `git log -3 --oneline` を確認し、`docs/claude-review.md`、`docs/codex-review-response.md`、`docs/release-audit.md` を読んでから必要な差分だけ参照してください。

今回の確認:

1. AppIconの実PNGがRGBになり、生成処理と公開前検査で後退を検出できるか。
2. 製品SWの初回保存失敗・再試行・更新失敗・連続更新・旧名整理に欠陥がないか。実行版と待機版の混在、他スコープの保存削除、失敗を隠す試験がないか。
3. 査読の2棋譜で、取り返せる駒を一方的な損とせず、無関係な正解手に救出を帰属していないか。釘付け・打ち駒表記・と金の文も確認。
4. 戦法の助言、リロード後の戻る、拡大振り返りの戻る、古い台詞タイマーが直っているか。製品版の実プレイ試験が手書きの記録に依存していないか。
5. #9は表示頻度を下げず、2スレッド・400ms・MultiPV 2の計測と、保守的な基準を維持する方針を明記しています。この判断に問題があれば、頻度だけでなく誤認の再現例と改善案を示してください。

5段階の台詞、一手一台詞、固定素材、音の種類・雷10%、タイトル全身・対局顔のみは維持。公開先未定、実機未検証、GPLとApp Store条件は既知で、今回解決済みとは扱っていません。

各指摘について「修正確認／残存／判断の再検討」を返してください。新規欠陥は重要度順に最大5件、再現手順と期待・実際を添えてください。既存ログは上書きせず、結果は `docs/claude-rereview.md` に記録し、要点を返してください。

利用量を抑えるため、テスト再実行は疑いのある対象に限定してください。全面的な改善案の列挙や全コードの読み直しは不要です。4領域を再評価する場合は、未検証を合格扱いせず根拠と残条件を記載してください。

---
