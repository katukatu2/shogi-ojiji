# Claudeの18指摘への対応

2026-09-10。査読対象だった `e0b2b69` からの修正。原文の `docs/claude-review.md` は変更せず保存した。

**17項目にコード・試験・資料の修正を行った。#9は実測を追加して表示基準を維持した判断で、表示頻度そのものは減らしていない。公開可能・4領域8点とはまだ判定しない。**

## 指摘ごとの回答

| # | 対応と根拠 |
|---|---|
| 1 | iOS AppIconの生成処理をRGB PNGへ変更し、実ファイルを再生成。IHDRは1024×1024、8bit、colorType 2。独立したPNGデコーダで全画素を再読し、元の描画と色が一致する試験も追加。App Store Connectへの実アップロードは未実施 |
| 2 | 初回の保存失敗ではSWをヘッダー補助として有効化し、オンラインのエンジンを維持。未保存の案内と通信復帰時の再試行を追加。部分保存を完成扱いしない。既に動く版がある場合は、不完全な更新を採用せず旧版を保つ |
| 3 | 駒を取った後の合法な取り返しを調べ、同等交換・取り返せる取り・一方的な損を区別。救出の説明には「指した手の後で実際に駒損になる」という前提を追加。Claude採取の2棋譜、釘付けで取り返せない例、守りを外したために取られる既存例を検証 |
| 4 | 戦法別の助言を結果画面へ復元。5戦法すべての一般助言に表示関数から到達することを試験。銀の進出を述べる助言は実盤に条件を付け、角交換・中飛車の助言は条件を明示する文へ修正。前回の一律文への置換はCodexの修正漏れだった |
| 5 | 再読み込み前の同一URL内の履歴段数を起動時に畳む。設定・対局それぞれでリロード後の「戻る」1回が、アプリを開く前のプライバシーページへ戻ることをChromium/WebKitで確認 |
| 6 | 実際の製品SWソースの分岐を試験。保存成功・失敗5種・再試行・HTML/JSの同一版・Range・更新失敗・連続更新・旧名整理を検証。製品ビルドの実ブラウザー試験も追加 |
| 7 | 実在する設定の「‹ タイトル」を必須で取得・クリックし、タイトルへの遷移を確認してからすぐ「はじめる」を押す。対象が見つからなければ試験を失敗させる |
| 8 | 拡大振り返り中の「戻る」は拡大だけを閉じる。結果・免状・昇級の画面を残し、次の「戻る」でタイトルへ。保存された成績も変わらないことを検証 |
| 9 | 2スレッド・400ms・MultiPV 2の到達深さを実測。初期16・10手目13という実行があり、負荷・局面で変わる。14は到達保証ではなく保守的な表示基準と明記。中盤で「目安」が頻繁に出ることは残る。閾値を下げて不確実さを隠す変更はしていない。表示方法の再査読を依頼 |
| 10 | Android versionNameを1.0から0.1.0へ揃え、package・Android・iOS全構成の版番号とビルド番号を事前検査。iOSの番号更新を公開手順に追加。生成物だけを確認する `release:artifacts` をCIへ追加 |
| 11 | 相手の応手を「指した▲○○には、△○○で…」と明示し、直前の正解手との混同を除去。現在の共通表記は元々「と金」だったため、二重の「と」は現HEADで再現しなかったが、説明生成まで通す回帰試験を追加 |
| 12 | エンジン判定・簡易判定の応手は、実際にその手を指す直前の盤面を共通表記へ渡す。盤上の同種駒が動ける場合だけ「打」を付ける例と、不要な例を検証 |
| 13 | 各資源にSHA-256を付け、既存の完成版と内容が同じ資源は検証後に再利用。実行版＋最新待機版だけを残す。実ブラウザーのA→失敗B→B→C更新で、既存タブはAのまま、共有資源の通信1回、保存はA/C/管理情報、全タブを閉じるとCに切り替わることを確認 |
| 14 | 新版を有効化した際に、旧名 `ojiji-v4` などのうち同じ配信範囲のアプリ資源を整理。他のURL・他アプリ・他スコープの保存は残す試験を追加 |
| 15 | タイトル・設定・新規対局で台詞タイマーと一手単位の状態をリセット。古いコールバックも世代を確認して捨てる。前の終了時刻を過ぎた後でも新しい対局の台詞が出ることをChromium/WebKitで確認 |
| 16 | 固定深さの正しさの試験を残したまま、本体の2スレッド・400ms・MultiPV 2で初期と駒がぶつかる局面の実エンジン試験を追加。合法手・評価値・PV本数を検査し深さを記録。時間依存の深さを固定値で断定しない |
| 17 | 改名時の変更先一覧にiOS `Info.plist` の `CFBundleDisplayName` を追加 |
| 18 | 開発フックのない製品版で、初手→実盤に合わせて角を捨てる手→本物のエンジン判定→継続→相手の応手→投了→結果→実際の記録の振り返り、をUIだけで操作。指す前・後の81マスと実対局の盤の違いを確認。成績も1局だけ。詰み・昇級の長い全対局E2Eまで網羅したとは言わない |

## 検証範囲

- Node 22.15.0、型チェック成功。
- 単体304件、15ファイル。既定のファイル並列を有効にしたまま成功。
- 操作試験40件成功（再試行なし）、Chromium/WebKit。追加した履歴・拡大振り返り・台詞の競合を含む。
- 製品PWA試験14件成功（再試行なし）、Chromium、配信ヘッダーあり／なし。ルート／サブフォルダ、保存障害からの復旧、実対局からの振り返り、複数版の更新を含む。
- 製品ビルド、Android/iOSへのCapacitor sync成功。ネイティブのAPK・IPAビルドは未実施。
- `release:artifacts` は成功。公開情報を含む `release:check` は未定4箇所で意図どおり失敗。
- RGBアイコンを目視し、全画素の復号比較も成功。元の顔・体・駒音・雷・エンジンは変更なし。

一括検証の最終件数と残作業は [release-audit.md](release-audit.md) に記録する。WindowsでのPlaywright WebKitは、iPhone実機またはWKWebViewの試験ではない。

## 残るリリース条件

公開先URL・連絡先、対応ソースと配布条件の確認、Android/iOSのネイティブビルドと実機、本格エンジンのWKWebViewでの動作、署名とストア提出が未完了。前回のClaude評価（指導6・ゲーム6・リリース4・品質保証5）は、修正担当者の自己判断で8へ上げない。
