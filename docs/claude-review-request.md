# Claudeへの限定査読依頼

以下をClaude Codeへ渡してください。リポジトリのファイルを参照できる環境向けです。

---

「将棋オジジの定石指南」の査読をお願いします。Codexが実装とテストを担当しました。今回の役割は査読です。アプリの実装ファイルは編集せず、同時編集による衝突を避けてください。

対象:
`C:\Users\sonot\Documents\Codex\2026-09-04\shogi-ojiji`
起点: `a9bf6fd`。まず `git status --short`、`git log -3 --oneline`、`docs/release-audit.md` を確認してください。必要な差分は `git diff a9bf6fd HEAD`、未コミット差分があれば `git diff` です。ログ全体の貼り付けやコード全体の再読は不要です。

確定方針:
- 今回はiPhone App Store・Android・Webが対象。
- 正式名は「将棋オジジの定石指南」。「（仮）」は外す。
- 公開先URL・連絡先は未定。仮の情報で埋めない。
- 5段階の台詞、一手一台詞、声なし、駒音と雷のみ、雷音量10%、タイトル全身・対局顔のみ、固定素材とやねうら王を維持する。

最初の1回は次に絞ってください。
1. `public/coi-serviceworker.js` と `scripts/prepare-offline.mjs`: 初回保存、音声Range、更新時に異なる版が混ざらないか、他アプリの保存を壊さないか。
2. `src/ui/navigation.ts` と `src/main.ts` の接続、抽出した画面: 戻る操作・再戦・昇級・非同期応手で状態が壊れないか。
3. `src/ai/engine.test.ts`: 固定深さへの変更とSFEN修正が、単に不具合を隠していないか。
4. iOSの構成と公開資料: 簡易判定を本格判定と同等に扱う記載や、未検証を完了とする箇所がないか。
5. 引き継ぎ時の指導・ゲーム完成度の修正について、既存の `src/style/judge.ts`、`src/game/review.ts` と関連テストを必要な箇所だけ読み、4領域の8点判定に不足する根拠を示す。

出力:
- 最初に、リリースを止める新規の問題があるか。
- 新規指摘は重要度順に最大5件。各指摘に「ファイルと行、具体的な再現手順、期待と実際、修正案」を記す。
- 公開先未定、実機未検証、GPLとApp Store条件の確認待ちは既知。新発見と混ぜない。
- 4領域を再評価する場合、点数と根拠・未検証範囲を併記する。未検証を合格扱いしない。
- 問題を見つけなければ「この範囲では新規指摘なし」として終える。改善案を無制限に増やさない。

結果は `docs/claude-review.md` だけに記録し、要点を返してください。テストの再実行は疑いのある対象に限定してください。Codex側の最終検証は単体273件、操作34件、PWA4件成功です。

---

Claudeの返答をCodexへ渡すときは「docs/claude-review.md の指摘を再現し、必要な修正と検証を進めてください」で続行できます。
