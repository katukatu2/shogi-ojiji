# Claudeへのフラグ検査の限定確認依頼

今回の修正は作業フォルダーに保存済み・未コミットです。Git管理フォルダーの書き込みが追加許可後も拒否されました。`b0b3925` からの作業差分と追加された未追跡文書を対象にしてください。

`docs/claude-ci-gate-review.md` の新規指摘1件を修正しました。対象は `b0b3925` 以降のCLI検査と、併用文言・CIの終了猶予・通し試験の接続先だけです。詳細は `docs/codex-ci-gate-response.md`。

`--all-style` など不明なフラグが、終了コード1・エンジン未起動・既存ログ保持となることを確認してください。既知フラグと区切り `--`、全戦法処理、例外後の継続を壊していないか、重複と併用の診断も対象です。回帰4ケースは修正前に失敗し、修正後のCLI試験11件と単体335件は成功しています。

CI上限25分に対してPlaywright全体上限は20分、接続先は待ち受けと同じ127.0.0.1に揃えました。製品通しは変更後79手・56.7秒で成功しています。前回独立確認済みの指導内容全体や通し処理の切り出しを再査読する必要はありません。

実装は編集せず、確認結果を `docs/claude-cli-review.md` に追加してください。GitHub上のCI・実機・ネイティブ成果物等は未確認のままです。現在の独立評価8/7/7/7を、今回の設定変更だけで引き上げないでください。
